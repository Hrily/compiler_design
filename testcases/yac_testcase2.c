#include <stdio.h>

int main () {
    // Invalid declaration
    int a b;

    // Invalid expression
    a = a + b -;

    // Invalid assignment
    a =;

    // Invalid iteration
    for(){
        a = b;
    }

    // Invalid condition
    else {
        a = b;
    }
}
